<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 1. 扫描注解：自动发现和注册Bean -->
    <context:component-scan base-package="com.spring.mvc.again.controller"/>

    <!-- 2. 配置视图解析器 JSP-->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- 3. 配置一个简单的UserController Bean -->
    <bean id="userController" class="com.hxl.controller.UserController"/>

    <!-- 4. 配置拦截器 -->
    <mvc:interceptors>

        <!-- 拦截器1：全局拦截器，拦截所有请求 -->
        <bean class="com.hxl.interceptor.GlobalInterceptor"/>

        <!-- 拦截器2：针对特定路径的拦截器 -->
        <mvc:interceptor>
            <mvc:mapping path="/user/**"/>
            <mvc:exclude-mapping path="/user/login"/>
            <mvc:exclude-mapping path="/user/register"/>
            <bean class="com.hxl.interceptor.AuthInterceptor"/>
        </mvc:interceptor>

        <!-- 拦截器3：日志拦截器 -->
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <bean class="com.hxl.interceptor.LoggingInterceptor"/>
        </mvc:interceptor>

        <!-- 拦截器4：管理员路径拦截 -->
        <mvc:interceptor>
            <mvc:mapping path="/admin/**"/>
            <bean class="com.hxl.interceptor.AdminInterceptor"/>
        </mvc:interceptor>

    </mvc:interceptors>

</beans>